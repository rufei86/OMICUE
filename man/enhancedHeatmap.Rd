% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{enhancedHeatmap}
\alias{enhancedHeatmap}
\title{Publication-style heatmap of GSEA scores, saved to file}
\usage{
enhancedHeatmap(
  mat.heatmap,
  filename = "Integrated_Pathway_Heatmap.pdf",
  low = NULL,
  high = NULL,
  pwd,
  pathway_name = "Pathway",
  category_name = "shared",
  column_pattern = "GScore",
  col_gradient = "RdYlBu"
)
}
\arguments{
\item{mat.heatmap}{A data frame with a \code{pathway_name} column, a
\code{category_name} column, and one or more score columns matching
\code{column_pattern}.}

\item{filename}{Output file name. Defaults to
\code{"Integrated_Pathway_Heatmap.pdf"}.}

\item{low, high}{Optional numeric color-scale bounds; default to the 10th
and 90th percentiles of the score columns.}

\item{pwd}{Directory the file should be saved to.}

\item{pathway_name}{Column in \code{mat.heatmap} used as row names. Defaults
to \code{"Pathway"}.}

\item{category_name}{Column in \code{mat.heatmap} used for row annotation and
ordering. Defaults to \code{"shared"}.}

\item{column_pattern}{Pattern used to select score columns. Defaults to
\code{"GScore"}.}

\item{col_gradient}{RColorBrewer palette name. Defaults to \code{"RdYlBu"}.}
}
\value{
Invisibly, \code{NULL}. As a side effect, writes the heatmap file.
}
\description{
Selects columns matching \code{column_pattern} (typically \code{"GScore"}), orders
rows within each category, and renders a \code{\link[pheatmap:pheatmap]{pheatmap::pheatmap()}} to
\code{pwd/filename}.
}
