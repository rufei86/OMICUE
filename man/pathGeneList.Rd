% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment.R
\name{pathGeneList}
\alias{pathGeneList}
\title{Build a gene-list summary table for a set of pathway IDs}
\usage{
pathGeneList(ids, gset, gse_res, gage_in, gene_db)
}
\arguments{
\item{ids}{Character vector of pathway IDs (matched against
\code{names(gset)}); \code{NA} entries are dropped.}

\item{gset}{A named list of gene sets (e.g. \code{geneSets} slot from a
clusterProfiler / ReactomePA result).}

\item{gse_res}{A data frame with columns \code{Pathway} and \code{PathID} used to
look up the human-readable description for each ID.}

\item{gage_in}{A named numeric vector (e.g. a ranked gene list) whose
names are used to restrict each pathway's genes to those present.}

\item{gene_db}{An annotation database object (e.g. an \code{OrgDb}), passed to
\code{\link[AnnotationDbi:mapIds]{AnnotationDbi::mapIds()}}.}
}
\value{
A data frame with columns \code{Pathway.ID}, \code{Description}, \code{Genes}
(comma-separated gene symbols).
}
\description{
For each ID in \code{ids}, looks up the matching gene set, its description in
\code{gse_res}, and the subset of genes present in \code{gage_in}, returning a
single tidy table.
}
