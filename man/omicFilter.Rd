% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manipulation.R
\name{omicFilter}
\alias{omicFilter}
\title{Filter omic structures and metadata}
\usage{
omicFilter(
  data,
  factor,
  omic_names = c("olink"),
  subjectid = NA,
  sampleid_metadata = "SampleID",
  sampleid = "SampleID",
  filters
)
}
\arguments{
\item{data}{A list-like object containing \code{metadata} and one or more omic
tables.}

\item{factor}{Character scalar, column name in \code{metadata} used for
filtering.}

\item{omic_names}{Character vector of names in \code{data} that contain omic
tables. Defaults to \code{c("olink")}.}

\item{subjectid}{Character scalar, column name for subject ID in
\code{metadata} (use \code{NA} to skip subject-level deduplication).}

\item{sampleid_metadata}{Character scalar, column name for sample ID in
\code{metadata}. Defaults to \code{"SampleID"}.}

\item{sampleid}{Character scalar, column name for sample ID in the omic
tables. Defaults to \code{"SampleID"}.}

\item{filters}{Vector of values used to filter \code{factor}.}
}
\value{
The modified \code{data} list with filtered \code{metadata} and omic tables.
}
\description{
Filter omic structures and metadata
}
