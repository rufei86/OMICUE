% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathway-analysis.R
\name{PathwayAnalysis}
\alias{PathwayAnalysis}
\title{Run differential-feature and pathway-style comparisons across groups}
\usage{
PathwayAnalysis(
  factor,
  omic_name,
  level_id = NULL,
  name_suffix = "",
  method = c("wilcox", "regression", "rankregression", "logistic"),
  empirical = FALSE,
  n_perm = 1000,
  covariates = NA,
  reference = NULL,
  id_name,
  data,
  gene_db,
  p_adjust_method = "none",
  p_gsea_adjust_method = "fdr",
  min_sig = 20,
  species = "hsa",
  plimit = 0.05,
  plimit_test = 0.05,
  oid_suffix = "-OID",
  n_cores = round((parallel::detectCores() - 6)/2),
  pwd
)
}
\arguments{
\item{factor}{Character scalar, column name in \code{data$metadata} used to
define groups (or the response, for regression methods).}

\item{omic_name}{Character scalar, name of the omic table in \code{data}.}

\item{level_id}{Optional character scalar, random-effect grouping column
passed through to \code{\link[=LogReg]{LogReg()}}.}

\item{name_suffix}{Character scalar appended to the output directory
name.}

\item{method}{One of \code{"wilcox"}, \code{"regression"}, \code{"rankregression"},
\code{"logistic"}.}

\item{empirical}{Logical; use permutation p-values (methods that support
it).}

\item{n_perm}{Number of permutations when \code{empirical = TRUE}.}

\item{covariates}{Character vector of covariate column names, or \code{NA}.}

\item{reference}{Optional character scalar; if supplied, all other levels
of \code{factor} are compared against this reference level instead of every
pairwise combination.}

\item{id_name}{Character scalar, sample ID column shared between
\code{data[[omic_name]]} and \code{data$metadata}.}

\item{data}{A list-like object containing \code{metadata} and one or more omic
tables.}

\item{gene_db}{An annotation database object (e.g. an \code{OrgDb}), passed to
\code{\link[AnnotationDbi:select]{AnnotationDbi::select()}}.}

\item{p_adjust_method}{P-value adjustment method for the per-comparison
feature tests, passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}.}

\item{p_gsea_adjust_method}{P-value adjustment method reserved for
downstream GSEA steps (kept for interface parity with
\code{\link[=Enrich_GO]{Enrich_GO()}}/\code{\link[=Enrich_KEGG]{Enrich_KEGG()}}/\code{\link[=Enrich_REACTOME]{Enrich_REACTOME()}}).}

\item{min_sig}{Minimum gene set size reserved for downstream GSEA steps.}

\item{species}{Species code reserved for downstream GSEA steps.}

\item{plimit}{P-value cutoff reserved for downstream GSEA steps.}

\item{plimit_test}{P-value cutoff used to filter significant features
from the per-comparison tests.}

\item{oid_suffix}{Character scalar, suffix separating a feature's gene ID
from its assay/probe identifier (e.g. \code{"-OID"}).}

\item{n_cores}{Number of cores to use for regression-based methods.}

\item{pwd}{Character scalar, directory in which output subfolders and
CSVs are written.}
}
\value{
Invisibly, a list with \code{sig_gene_all} and \code{de_all}, the per-
comparison significant-feature and full differential-expression tables.
As a side effect, writes \verb{pwd/<factor><name_suffix> - Pathway Analysis/} with subfolders and summary CSVs.
}
\description{
For every pairwise (or reference-vs-other) combination of levels of
\code{factor}, runs the requested per-feature test (Wilcoxon, logistic,
linear/gaussian regression, or rank regression), annotates significant
features with Entrez IDs, and writes summary CSVs to disk.
}
