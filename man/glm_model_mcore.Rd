% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/differential-expression.R
\name{glm_model_mcore}
\alias{glm_model_mcore}
\title{Fit a single-predictor GLM or GLMM, optionally in parallel with a
permutation p-value}
\usage{
glm_model_mcore(
  factor,
  covariates,
  data_i,
  x,
  family = stats::binomial(),
  empirical = FALSE,
  n_perm = 1000,
  seed = 123,
  level_id = NULL,
  n_cores = parallel::detectCores() - 3
)
}
\arguments{
\item{factor}{Character scalar, name of the response column.}

\item{covariates}{Character vector of covariate column names, or \code{NA} to
fit an unadjusted model.}

\item{data_i}{Data frame containing \code{factor}, \code{covariates}, and \code{x}.}

\item{x}{Character scalar, name of the predictor column.}

\item{family}{A family object as used by \code{\link[stats:glm]{stats::glm()}}, e.g.
\code{binomial()} or \code{gaussian()}.}

\item{empirical}{Logical; if \code{TRUE}, compute an empirical p-value via
label permutation instead of the asymptotic p-value.}

\item{n_perm}{Number of permutations used when \code{empirical = TRUE}.}

\item{seed}{Random seed for permutations.}

\item{level_id}{Optional character scalar, name of a grouping column to
add as a \code{(1 | level_id)} random intercept.}

\item{n_cores}{Number of cores to use for permutations.}
}
\value{
A list with \code{coefficient} (named numeric vector) and
\code{empirical_p_value}.
}
\description{
Same purpose as \code{\link[=glm_model]{glm_model()}}, but supports an optional random-effect
\code{level_id} (fit via \code{lme4}/\code{lmerTest}) and runs permutations in parallel
via \code{\link[parallel:mclapply]{parallel::mclapply()}}.
}
