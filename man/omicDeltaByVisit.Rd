% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manipulation.R
\name{omicDeltaByVisit}
\alias{omicDeltaByVisit}
\title{Compute visit-specific change from baseline for multiple omics}
\usage{
omicDeltaByVisit(
  data,
  omic_names,
  subjectid,
  visitid,
  baseline_level,
  metadata_name = "metadata",
  sampleid = NA,
  feature_cols = NULL
)
}
\arguments{
\item{data}{A list-like object containing one or more omic tables plus a
\code{metadata} element (see \code{metadata_name}).}

\item{omic_names}{Character vector of names in \code{data} that contain omic
tables.}

\item{subjectid}{Column name for subject ID.}

\item{visitid}{Column name for visit / timepoint.}

\item{baseline_level}{Value of \code{visitid} indicating baseline (e.g.
\code{"Baseline"} or \code{0}).}

\item{metadata_name}{Name of the element in \code{data} holding sample-level
metadata. Defaults to \code{"metadata"}.}

\item{sampleid}{Column name for sample ID (optional; can be \code{NA}).}

\item{feature_cols}{Optional character vector of feature (gene/protein)
columns. If \code{NULL}, all numeric columns except id/visit/sample are used.}
}
\value{
Modified \code{data} list; for each omic table:
\itemize{
\item baseline rows kept as-is (no deltas),
\item for each non-baseline visit, new columns named
\verb{delta-<visit>-<feature>} with visit - baseline,
\item delta columns that are all NA are removed.
}
}
\description{
For each omic table in \code{data}, computes the change from baseline
(\code{value - baseline_value}) for every numeric feature, at every non-baseline
visit, and returns the result in wide format (one row per subject, one
column per \verb{delta-<visit>-<feature>}).
}
