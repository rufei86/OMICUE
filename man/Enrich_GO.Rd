% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment.R
\name{Enrich_GO}
\alias{Enrich_GO}
\title{Gene Set Enrichment Analysis on GO Biological Process terms}
\usage{
Enrich_GO(sigs, gene_db, plimit, p_adjust_method)
}
\arguments{
\item{sigs}{A named, sorted numeric vector (e.g. ranked log-fold-changes)
as expected by \code{geneList} in \code{\link[clusterProfiler:gseGO]{clusterProfiler::gseGO()}}.}

\item{gene_db}{An \code{OrgDb} annotation object.}

\item{plimit}{P-value cutoff passed to \code{pvalueCutoff}.}

\item{p_adjust_method}{P-value adjustment method passed to
\code{pAdjustMethod}.}
}
\value{
A list with elements \code{gse_GO} (the raw enrichResult), \code{result_GO}
(tidy summary data frame with \code{PathID}, \code{Pathway}, \code{Score}, \code{Pval},
\code{GScore}), \code{go_gset} (the underlying gene sets) and \code{go_ids}.
}
\description{
Wraps \code{\link[clusterProfiler:gseGO]{clusterProfiler::gseGO()}} (ontology \code{"BP"}) with a \code{tryCatch} that
retries once on warnings, then simplifies redundant terms and returns a
tidy summary table.
}
