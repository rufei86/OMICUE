% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{AdvancedVolcano}
\alias{AdvancedVolcano}
\title{Volcano plot with pathway-colored gene highlighting}
\usage{
AdvancedVolcano(
  data,
  x,
  y,
  path_list,
  path_name = "Pathway.ID",
  legend_position = "top",
  path_id = "Genes",
  raw_pvalue = TRUE,
  point_size = 2.5,
  text_size = 2.5,
  line_size = 0.6,
  x_cutoff = c(-0.2, 0.2),
  y_cutoff = -log10(0.01),
  id_col = "GeneID",
  xlab = "Log2FC",
  ylab = "-log10(P-Value)",
  cols = grDevices::palette(),
  max.overlaps = 30
)
}
\arguments{
\item{data}{A data frame containing at least \code{x}, \code{y}, and \code{id_col}.}

\item{x}{Character scalar, column name to use on the x-axis (typically
log fold-change).}

\item{y}{Character scalar, column name to use on the y-axis (a p-value,
or already-transformed \code{-log10(p)} if \code{raw_pvalue = FALSE}).}

\item{path_list}{A data frame with one row per pathway, containing
\code{path_name}, \code{"Description"}, and \code{path_id} (comma-separated gene IDs).}

\item{path_name}{Column name in \code{path_list} giving the pathway
identifier/label used for coloring. Defaults to \code{"Pathway.ID"}.}

\item{legend_position}{Passed through conceptually; retained for
interface parity (legend position is set via \code{ggplot2::theme()}
internally).}

\item{path_id}{Column name in \code{path_list} with comma-separated gene IDs.
Defaults to \code{"Genes"}.}

\item{raw_pvalue}{Logical; if \code{TRUE}, \code{y} is \code{-log10()}-transformed.}

\item{point_size}{Base point size.}

\item{text_size}{Label text size.}

\item{line_size}{Cutoff line width.}

\item{x_cutoff}{Length-2 numeric, vertical dashed-line positions.}

\item{y_cutoff}{Numeric, horizontal dashed-line position.}

\item{id_col}{Column name in \code{data} with the gene/feature identifier.}

\item{xlab, ylab}{Axis labels.}

\item{cols}{Named vector of colors keyed by pathway label.}

\item{max.overlaps}{Passed to \code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Volcano plot with pathway-colored gene highlighting
}
